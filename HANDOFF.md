# 智鉴安澜项目交接说明

## 1. 当前项目定位

智鉴安澜是面向普通用户的多模态安全研判系统。用户可提交文本、图片、音频和链接，系统以智能体协同方式完成内容鉴真、攻击意图分析、证据回溯与安全报告生成。当前版本优先服务国赛答辩和演示视频，企业信息流接入仅作为未来扩展能力。

## 2. 目录说明

- `frontend/`：React + Vite 前端，当前展示工作的重点。
- `backend/`：Spring Boot 服务，包含用户、研判记录、报告和知识库等接口。
- `ml_service/`：Python 多模态处理与智能体实验代码。
- `frontend/public/demo/`：当前演示使用的虚构图片素材。

## 3. 前端快速启动

```bash
cd frontend
npm install
npm run dev
```

浏览器访问 `http://localhost:5173`。需要直接查看受保护页面时，可先从登录页进入；当前页面主要路由如下：

- `/detect`：智能体研判与材料提交入口。
- `/analysis`：综合研判、内容鉴真、攻击意图链和证据图谱。
- `/reports`：安全报告、主张审计与审计日志。
- `/knowledge`：原有知识库。
- `/user`：简化后的个人中心。

前端构建命令：

```bash
npm run build
```

## 4. 当前已完成内容

- 继承原项目暗色视觉风格，重构智能体研判、研判详情、安全报告和个人中心。
- 本地隐私预检展示本地 OCR、规则校验、字段脱敏和 EXIF 清理。
- 综合研判内整合多智能体协同过程，不再设置独立的“协同核验”分页。
- 内容鉴真包含证件局部异常定位、残差/热力展示、音频频谱和异常时段。
- 攻击意图链包含六阶段主链、多模态证据轨、实际结果和条件推演分支。
- 证据图谱包含五层、21 个节点和25条支持/关联/冲突关系。
- 深度研判默认视为报告已生成，可直接进入安全报告页面。

## 5. 演示数据说明

研判详情和报告页面当前使用固定演示案件：虚构银行工作人员资质图片、通话音频及演示钓鱼链接。演示域名使用 `.example`，图片带有“演示样例”标识，不对应真实机构。

目前尚未制作“用户上传材料后，聊天区逐步输出固定研判结果”的拍摄逻辑。后续确定视频脚本后，建议主要修改：

- `frontend/src/pages/Detect/AiChatPanel.jsx`
- `frontend/src/pages/Detect/Detect.jsx`
- `frontend/src/pages/Analysis/Analysis.jsx`
- `frontend/src/pages/Reports/Reports.jsx`

不要提前接入真实模型阻塞视频拍摄；可先按脚本实现分阶段定时输出，再补后端或模型接口。

## 6. 后端与模型服务

后端使用 Java 17。启动前需准备 MySQL、Redis，并设置 `MYSQL_PASSWORD`；默认端口为 `8889`。

```powershell
cd backend
.\mvnw.cmd spring-boot:run
```

模型服务的私密配置放在 `ml_service/.env`，该文件不会提交。首次配置可复制 `ml_service/.env.example`，填写 `DASHSCOPE_API_KEY` 和模型名称。虚拟环境、模型缓存和构建产物均不进入仓库。

## 7. 协作注意事项

- `main` 保持可演示状态，新功能建议从独立分支开发后合并。
- 不要提交 `.env`、API Key、数据库密码、`node_modules/`、`venv/`、`target/` 或 `dist/`。
- 修改固定案件时，需要同步研判详情、报告、材料名称、Evidence ID 和风险数字，避免视频中前后矛盾。
- 当前前端生产构建已通过；全量 ESLint 仍有部分旧组件历史问题，集中在旧知识库和旧侧栏，不是本轮页面新增错误。
