# =====================
# 外部系统依赖（需手动安装）
# =====================
# ffmpeg — 视频音轨提取依赖，preprocessing/video_preprocessor.py 会自动检测是否可用。
# 未安装时视频音轨提取会静默跳过，不影响其他功能。
# Windows 安装方式：https://ffmpeg.org/download.html 或 winget install ffmpeg
#
# webrtcvad — 语音活动检测（VAD），未安装时自动回退到能量阈值 VAD。
# Windows 安装可能需要 C++ 编译器（Visual Studio Build Tools）：
#   pip install webrtcvad-wheels  （推荐，预编译 wheel）

# =====================
# 核心深度学习框架
# =====================
torch>=2.1.0
torchaudio>=2.1.0
transformers>=4.40.0
accelerate>=0.29.0

# =====================
# 数据处理
# =====================
numpy>=1.24.0
scikit-learn>=1.3.0
pandas>=2.0.0
tqdm>=4.65.0

# =====================
# API 服务
# =====================
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0
httpx>=0.27.0           # FastAPI 测试客户端 / 异步 HTTP 请求
python-multipart>=0.0.9 # FastAPI 文件上传（multipart/form-data）
aiofiles>=23.0.0        # FastAPI 异步文件读写

# =====================
# 阿里云 DashScope（qwen-omni-turbo 全模态 LLM / 向量化 / ASR / OCR）
# =====================
dashscope>=1.25.0

# =====================
# 音频处理
# =====================
soundfile>=0.12.0
librosa>=0.10.0
# webrtcvad 在 Windows 上需要 C 编译器，安装失败时代码会自动回退到能量阈值 VAD
# webrtcvad>=2.0.10

# =====================
# 图像处理
# =====================
Pillow>=10.0.0
opencv-python>=4.8.0

# =====================
# 知识图谱 / 向量检索
# =====================
neo4j>=5.0.0
faiss-cpu>=1.7.4

# =====================
# 融合判别层 — 记忆存储
# =====================
redis>=5.0.0
psycopg2-binary>=2.9.9
asyncpg>=0.29.0

# =====================
# Prompt 优化（可选）
# =====================
# dspy-ai>=2.4.0

# =====================
# 测试
# =====================
pytest>=7.4.0
